using UnityEngine;
using System.Collections;

public enum ButtonType { Gravity, Health, Spawn };

public class ObjectType : MonoBehaviour {

	// Use this for initialization
	void Start () {
	    
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
